-- Math Task #1
SELECT 2 + 2; --Adds Numbers

SELECT 'a' || 'b'; --Joins 2 strings together

SELECT 10 / 3; --Integer division

SELECT 10.0 / 3; --Decimal division

-- Math Task #2

SELECT 10 / 3, 10 % 3; --Quotient (integer division) and remainder (modulo)